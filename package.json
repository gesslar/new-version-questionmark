{
  "name": "new-version-questionmark",
  "version": "1.8.1",
  "description": "Automatic version difference detection action for GitHub.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "ncc build src/index.js -o action",
    "update": "npx -y npm-check-updates -u && npm install",
    "version": "npm run build && git add action",
    "lint": "eslint src/",
    "patch": "npm version patch",
    "minor": "npm version minor",
    "major": "npm version major"
  },
  "keywords": [],
  "author": "gesslar",
  "license": "0BSD",
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@gesslar/toolkit": "^5.8.0",
    "compare-versions": "^6.1.1"
  },
  "devDependencies": {
    "@gesslar/uglier": "^2.4.1",
    "@vercel/ncc": "^0.44.1",
    "eslint": "^10.7.0"
  }
}
